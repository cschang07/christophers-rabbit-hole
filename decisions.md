# Decisions

- 2026-07-20: `/board` did not show the new `未來規劃` column after OrbStack restart because the running production Docker images were still built before `c569c32`; fixed by rebuilding/recreating `backend` and `web` from a clean `c569c32` worktree, with future-status API and visible board smoke verified.
- 2026-07-20: Deploying clean commit `c569c32` regressed the live yellow RabbitHole UI and left a web/backend mismatch; fixed by rolling both services back to their exact pre-deploy image pair, with desktop/mobile Board screenshots, card interaction, and zero console errors verified; prevention: never deploy a clean historical commit when the live image contains newer uncommitted UI—first reconstruct and diff the actual live source/image.
- 2026-07-20: Restored `未來規劃` on the yellow Board by proving the running-image/source delta was limited to `c569c32`, booting the exact candidate image pair in isolation, and deploying those tested images; the three pre-existing Future rows were never migrated or rewritten and are visible again.
- 2026-07-29: The 2026-07-27 Tailscale outage was caused by a Dev Zone deployment check running `tailscale status` and `tailscale dns status`; the standalone macsys CLI churned duplicate macOS Network Extension profiles and restarted with `WantRunning=false`, fixed by reconnecting at 23:12; prevention: do not use those CLI probes on this host until the stale macos profile is removed and revalidated—use `scutil --nc list` plus an application endpoint probe.
- 2026-08-20: News missed Grok 4.6 as a story because uncommitted deployed changes removed the AI/Taiwan themes and hid model results in a collapsed client-only table; fixed by restoring both daily themes, injecting verified official releases into AI article generation, and showing the model table open by default; prevention: a new model release must appear in the server-rendered News page and AI article smoke, not only in `/api/model-releases`.
